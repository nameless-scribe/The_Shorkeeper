# 数据库目录（默认 D:\SQLlite）
SHOREKEEPER_DB_DIR=D:\SQLlite
SHOREKEEPER_DB_PATH=D:\SQLlite\shorekeeper.db
SHOREKEEPER_WORKSPACE_DIR=D:\SQLlite\workspace

# ── 阿里云百炼（OpenAI 兼容接口）────────────────────────────
# 在百炼控制台 → API Key 页面复制：
#   - API Key（sk- 开头）
#   - OpenAI compatible 接入点（/compatible-mode/v1 结尾）

OPENAI_API_KEY=sk-你的百炼APIKey

# 接入点示例（请替换为你控制台里的完整地址）
OPENAI_BASE_URL=https://llm-o3kz1vn36c7rl7o7.cn-beijing.maas.aliyuncs.com/compatible-mode/v1

# 模型 ID（API 用小写 id，不是界面显示名 Qwen3.6-Plus）
DEFAULT_MODEL=qwen3.6-plus

# 可选：百炼部分模型不支持 stream_options，保持 false
INCLUDE_STREAM_USAGE=false
